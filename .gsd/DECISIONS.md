# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | Fixing seek behavior to trigger command queuing | bug-fix | Timeline seek should trigger commands via setIntensity/setChannelOverride | Modified _onTimelineScriptStep to call setIntensity/setChannelOverride only when step.isSeek is true. These methods update UI and send BLE commands. Natural playback just updates UI display without sending commands. | User wants seeking to actually apply the script settings to the device (queue commands), not just update UI. The isSeek flag distinguishes manual seek from natural playback. This ensures commands are only sent when user intentionally seeks, not every second during playback. | Yes | agent |
| D002 | Fixing timeline to properly distinguish natural progression vs seek | bug-fix | Timeline script steps explicitly set isSeek: false for natural progression, true only for manual seek | Modified _tick() to set isSeek: false explicitly, and seek() to set isSeek: true. Added logging to distinguish between natural progression (UI only) and seek (commands sent). | User needs clear distinction between natural playback (visual only) and manual seek (apply settings). The isSeek flag now explicitly indicates intent. Added debug logging to help diagnose any remaining issues with channel/intensity application. | Yes | agent |
| D003 | Fixing initial UI state for timeline script | bug-fix | On session start, explicitly read first timeline script entry and set UI to match initial channel/intensity | Added code to read first script instruction at time 0 and update UI controls to match before starting timeline tracking | The UI wasn't reflecting the initial script state when session started. Now it explicitly reads the first instruction (channel and intensity) and sets the UI controls to match, ensuring visual feedback matches what will be sent to the device. | Yes | agent |
| D004 | Adding live intensity updates during fade phases | ux | Timeline intensity slider updates during fade-down phases | Modified _tick() to detect intensity changes during fade instructions and trigger script step callback. Added isFadeUpdate flag and [Fade] log prefix. | During fade instructions, intensity changes gradually while the instruction stays the same. The UI intensity slider now updates to reflect the decreasing intensity during fades, giving users visual feedback of the ramp. Commands are NOT sent during fade updates - it's purely visual. | Yes | agent |
| D005 | Making fade-down functional by sending intensity commands | ux | Fade-down phases now send intensity commands to device as intensity decreases | Added logic to call setIntensity() during fade updates, sending actual commands to device during fade-down | User wants the fade-down to be functional, not just visual. During Sleep mode's final fade phase, as intensity drops from base level down to 1, each intensity change now sends a command to the device. This matches the user's expectation that the timeline controls the device during fades. | Yes | agent |
| D006 | Adding auto-start feature on BLE connection | ux | Auto-start session on device connection | Added this.startSession() call in the connected event handler to automatically start a session when device connects | User wants immediate session start on connection. This eliminates the extra step of clicking Start after connecting. Uses current mode/timer/intensity settings. | Yes | agent |
| D007 | Fixing natural timeline progression to control device | bug-fix | Natural phase transitions now trigger channel/intensity commands to device | Added logic to send commands during natural phase transitions (not just manual seek), so timeline progression actually controls the device | The timeline was only sending commands on manual seek, not during natural playback. When transitioning from 'Right' to 'Both (fade)' phase naturally, the device stayed on right channel. Now natural phase transitions trigger setIntensity() and setChannelOverride() just like manual seeks. | Yes | agent |
